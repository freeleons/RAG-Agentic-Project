"""Offline RAGAS-style eval harness. Not wired into the live app or CI —
run manually with `python -m server.eval.run_eval` after prompt/tool/model
changes, same trigger as the manual pass in docs/eval.md.
"""
